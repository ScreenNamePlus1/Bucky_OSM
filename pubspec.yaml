name: p2p_delivery_app
description: A Flutter P2P delivery bidding app.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^2.24.0
  firebase_auth: ^4.16.0
  cloud_firestore: ^4.14.0
  provider: ^6.1.1
  intl: ^0.18.1
  flutter_map: ^6.0.0
  latlong2: ^0.9.0
  geoflutterfire2: ^2.3.15
  flutter_osm_plugin: ^0.57.0
  retry: ^3.1.0
  shared_preferences: ^2.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true